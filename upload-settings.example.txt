# DistroKid upload-settings.txt (EXAMPLE / shipped defaults)
#
# Copy to upload-settings.txt next to the app if missing. Edit anytime.
#   Copy-Item upload-settings.example.txt upload-settings.txt
#
# Lines starting with # are comments. Keys are case-insensitive.
#
# ---------------------------------------------------------------------------
# Prices
# ---------------------------------------------------------------------------
album=9.99
track=0.99
#
# ---------------------------------------------------------------------------
# Release identity
# ---------------------------------------------------------------------------
releaser=(ezixen) records
#
# Songwriter(s) — ONE PERSON PER NUMBERED LINE (full legal name, any word count)
#   songwriter_1=Maria del Carmen Garcia Lopez Ruiz  → still ONE person
# songwriter_name_mode: auto | full_first
songwriter_1=George ezixen Lawrence
# songwriter_2=Another Person Full Name
songwriter_name_mode=auto
#
# artist = Primary artist (+ Apple credits fallback when no player_N= / producer_N=)
artist=ezixen
#
# ---------------------------------------------------------------------------
# Track flags
# ---------------------------------------------------------------------------
instrumental=on
explicit=off
#
# ---------------------------------------------------------------------------
# AI disclosure
# ---------------------------------------------------------------------------
# ai = off | on | both
# If ai_part_audio / ai_part_instruments / … are on while ai=off, uploader still answers Yes
# and selects Part of the audio (same as ai=both for instrumentals).
ai=off
ai_lyrics=off
ai_music=off
ai_all_audio=off
ai_part_audio=on
ai_part_instruments=on
ai_part_vocals=off
ai_artist_persona=human
#
# ---------------------------------------------------------------------------
# Apple Music credits
# ---------------------------------------------------------------------------
player_1=ezixen
# player_2=Another Performer Full Name
producer_1=ezixen
# producer_2=Another Producer Full Name
player_role=Unknown
producer_role=Executive producer
credit_artist=ezixen
credit_roles=Unknown, Executive producer
#
# ---------------------------------------------------------------------------
# Stores / free extras
# ---------------------------------------------------------------------------
stores_include_social=on
audiomack=on
release_date=today
#
# ---------------------------------------------------------------------------
# Important checkboxes (mandatory)
# ---------------------------------------------------------------------------
mandatory_checkboxes=on
#
# Genre stays manual. Album title = folder name rule (same as Bandcamp).
