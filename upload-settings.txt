# DistroKid upload-settings.txt
# Shipped defaults for ezixen releases (edit anytime; read on every run).
#
# Template with all options + comments: upload-settings.example.txt
# Lines starting with # are comments. Keys are case-insensitive.
#
# ---------------------------------------------------------------------------
# Prices
# ---------------------------------------------------------------------------
album=9.99
track=0.99
#
# ---------------------------------------------------------------------------
# Release identity
# ---------------------------------------------------------------------------
releaser=(ezixen) records
#
# Songwriter(s) — ONE PERSON PER NUMBERED LINE (any number of name words)
songwriter_1=George ezixen Lawrence
songwriter_name_mode=auto
#
# artist = Primary artist name on the release (+ Apple Music credits fallback)
artist=ezixen
#
# ---------------------------------------------------------------------------
# Track flags
# ---------------------------------------------------------------------------
instrumental=on
explicit=off
#
# ---------------------------------------------------------------------------
# AI disclosure (DistroKid popup)
# ---------------------------------------------------------------------------
# ai=off with ai_part_*=on still opens Yes + Part of the audio (loader treats as both)
ai=off
ai_lyrics=off
ai_music=off
ai_all_audio=off
ai_part_audio=on
ai_part_instruments=on
ai_part_vocals=off
ai_artist_persona=human
#
# ---------------------------------------------------------------------------
# Apple Music credits
# ---------------------------------------------------------------------------
player_1=ezixen
producer_1=ezixen
player_role=Unknown
producer_role=Executive producer
credit_artist=ezixen
credit_roles=Unknown, Executive producer
#
# ---------------------------------------------------------------------------
# Stores / free extras
# ---------------------------------------------------------------------------
stores_include_social=on
audiomack=on
release_date=today
#
# ---------------------------------------------------------------------------
# Important checkboxes (mandatory)
# ---------------------------------------------------------------------------
mandatory_checkboxes=on
#
# Genre stays manual. Album title = folder name rule (same as Bandcamp).
